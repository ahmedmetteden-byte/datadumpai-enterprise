# Placeholder environment for GitHub Actions `docker compose config` validation.
# Copied to .env on CI runners only. Production uses the server-managed .env file.
WEBHOOK_PORT=8001
ENVIRONMENT=test
AUTH_DEV_BYPASS=false
DATABASE_BACKEND=json
STORAGE_BACKEND=local
